smart_home:
  filter:
    include_entities:
      - light.back_yard_l

    exclude_domains:
      - automation
      - script
      - alert
      - fan
      - group
      - light
      - scene
      - sensor
      - switch
      - cover

  entity_config:
    